Modernizr.load([
  {
    test: Modernizr.input.placeholder,
    nope: ['<%= asset_path "jquery-placeholder/jquery.placeholder.js" %>'],
    callback: function () {
      $('input, textarea').placeholder();
    }
  }
]);