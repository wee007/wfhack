<% content_for :custom_styles,
  render('/shared/hero_custom_styles', image: @centre.code, target: '.header__hero')
%>

<%= pin_board(@stream) do |result| %>
  <%= tile result.kind,
    result: result,
    image: {href: result[:_links][:image][:href], title: result[:name]}
  %>
<% end %>