<br /><br /><br /><br /><br /><br />

<main class="main" id="main" role="main">

  <!-- [TODO] need to get the main detail view container into a layout -->
  <% if in_lightbox? %>
    <div class="page-overlay"></div>
    <article role="article" class="detail-view detail-view--event" tabindex="-1" role="dialog" aria-labelledby="detail-view__hdr" aria-hidden="true">
  <% else %>
    <article role="article" class="detail-view detail-view--event">
  <% end %>

    <%= render "shared/detail_view/controls" %>

    <div class="island island--top-btm detail-view__divider">
      <div class="container">

        <header class="detail-view__mrg-base">
          <h2 class="detail-view__hdr hn" id="detail-view__hdr"><%= @event.name %></h2>

          <div class="detail-view__meta islet islet--top-btm detail-view__divider detail-view__divider--top">
            <p class="detail-view__meta__date detail-view__subtle-txt">
              <time datetime="<%= Time.new(@event.start).strftime("%Y-%m-%d") %>"><%= Time.new(@event.start).strftime("%d %b %Y") %></time> to <time datetime="<%= Time.new(@event.start).strftime("%Y-%m-%d") %>"><%= Time.new(@event.finish).strftime("%d %b %Y") %></time>
            </p>
            <p class="detail-view__meta__location">
              <span class="icon icon--pin" aria-hidden="true"></span>
              <%= @event.location %>
            </p>
          </div>
        </header>

        <div class="detail-view__media detail-view__mrg-base">
          <%#= image_tag @event._links['image']['href'], alt: @event._links['image'] %>
        </div>

        <h3 class="detail-view__hdr-sub hn">Event Details</h3>
        <div class="detail-view__desc">
          <%= @event.description %>
        </div>

      </div>
    </div>

    <div class="island island--top-btm">
      <div class="container">

          <h3 class="detail-view__hdr-sub hn">Event Schedule</h3>

        </div>
    </div>

  <!-- [TODO] need to get the main detail view container into a layout -->
  </article>

</main>