<div class="detail-view__divider--col grid grid--gutterless grid--rev-stack grid--rev grid--flush">

  <!-- Product name, brand, price, location and contact -->
  <div class="grid__item non-palm-two-fifths">

    <!-- Product name, brand and price -->
    <header class="island detail-view__divider">
      <h1 class="detail-view__hdr hn detail-view__mrg-half" id="detail-view__hdr"><%= @product.name %></h1>
      <p class="detail-view__meta">
        <span class="detail-view__subtle-txt">from</span>
        <%= @product.retail_chain_name %>
        <%- if @product.is_discounted -%>
          <em class="detail-view__meta__sale-price">
            <span><%= Money.new(@product.price).format%></span>
            <%= Money.new(@product.sale_price).format %>
          </em>
        <%- else -%>
          <em><%= Money.new(@product.sale_price).format %></em>
        <%- end -%>
      </p>
    </header>
    <!-- //Product name, brand and price -->

    <!-- Store location -->
    <div class="map-micello map-micello--static detail-view__divider">
      <%= render partial: 'shared/map', locals: {store_id: @centre_stores.first.try(:id), interactive: false, load_immediately: true} %>
    </div>
    <!-- //Store location -->

    <!-- Contact store -->
    <div class="island island--fill island--center islet-tops detail-view__divider">
      <span class="hide-fully--non-palm">
        <a href="tel:<%= (@product.retail_chain.phone_no || "").gsub(/\s+/,'') %>" class="btn btn--full btn--main btn--main-alt2 btn--main-lrg"><span class="icon icon--phone icon--xlrg icon--spacing-lrg icon--flush-top" aria-hidden="true"></span><strong>Call <%= @product.retail_chain_name %></strong></a>
      </span>
      <p class="detail-view__check-avble hide-fully--palm"><span class="detail-view__subtle-txt">Check store availability</span> <em><span class="icon icon--phone icon--lrg icon--spacing" aria-hidden="true"></span> <%= @product.retail_chain.phone_no %></em></p>
    </div>
    <!-- //Contact store -->

    <!-- Store link -->
    <div class="island island--center detail-view__divider--palm">
      <%= link_to '#', class: "btn btn--pointer btn--full btn--full-left btn--main btn--main-alt2 btn--main-lrg btn--main-flanked", rel: "external nofollow" do %>
        Buy from <%= @product.retail_chain_name %>
        <span class="icon icon--arrow-right-big icon--lrg btn--pointer__icon" aria-hidden="true"></span>
        <span class="icon icon--link icon--xlrg icon--flush-top btn--pointer__icon" aria-hidden="true"></span>
      <%- end -%>
    </div>
    <!-- //Store link -->

  </div>
  <!-- //Product name, brand, price, location and contact -->

  <!-- Product images -->
  <div class="grid__item non-palm-three-fifths">

    <div class="island island--top-btm detail-view__divider--palm">
      <!-- Flexslider -->
      <div class="flexslider flexslider--lrg-img flexslider--gallery">
        <ul class="slides">
          <%- @product.images[0...4].each do |img| -%>
            <li><%= image_tag(img, alt: "Photo of #{@product.name}") %></li>
          <%- end -%>
        </ul>
      </div>
      <div class="flexslider flexslider--thumbs flexslider--gallery">
        <ul class="slides">
          <%- @product.images[0...4].each do |img| -%>
            <li><%= image_tag(img, alt: "Thumbnail of #{@product.name}") %></li>
          <%- end -%>
        </ul>
      </div>
      <!-- //Flexslider -->
    </div>

  </div>
  <!-- //Product images -->

  <span class="detail-view__divider--col-line hide-fully--palm"></span>

</div>

<div class="grid grid--gutterless grid--rev grid--flush">

  <!-- Available Colours -->
  <div class="detail-view__divider--top-non-palm grid__item non-palm-two-fifths">
    <%- if @product.available_colours.present? -%>
    <div class="island island--btm-palm">
      <h2 class="detail-view__hdr-sub hn">Available Colours</h2>
      <ul class="horiz-list horiz-list--comma"><!--
        <%- @product.available_colours.each do |colour| -%>
          --><li><strong><%= colour %></strong></li><!--
        <%- end -%>
      --></ul>
    </div>
    <%- end -%>
  </div>
  <!-- //Available Colours -->

  <!-- Description -->
  <div class="detail-view__desc detail-view__divider--top-non-palm grid__item non-palm-three-fifths">
    <div class="island">
      <h2 class="detail-view__hdr-sub hn">Description</h2>
      <%= raw(@product.detailed_description || @product.short_description) %>
    </div>
  </div>
  <!-- //Description -->

</div>

<!-- Availability -->
<div class="island detail-view__divider--top-non-palm">
  <%- if @centres.present? -%>
    <h2 class="detail-view__hdr-sub detail-view__mrg-off hn">Available at</h2>
    <ul class="detail-view__centres">
      <%- @centres.each do |centre| -%>
        <li>
          <%= link_to centre_product_path(centre_id: centre.code, retailer_code: @product.retailer_code, sku: @product.sku),
            class: "centres-list" do %>
            <span class="icon icon--pin icon--lrg icon--spacing-lrg" aria-hidden="true"></span>
            <%= centre.short_name %>
            <span class="icon icon--arrow-right icon--lrg" aria-hidden="true"></span>
          <%- end -%>
        </li>
      <%- end -%>
    </ul>
  <%- end -%>
</div>
<!-- //Availability -->

<%- content_for :javascript do -%>
  <!-- Run 'Flexslider' plugin -->
  <%= javascript_include_tag 'flexslider/jquery.flexslider' %>
  <script>
    $(window).load(function() {

      // Get the count of flexslider items
      var flexsliderItemsCount = $('.flexslider .slides li').length;
      // Flexslider thumbs
      var flexsliderThumbs = $('.flexslider--thumbs');

      // Only initialise flexslider if more than 2 items
      if (flexsliderItemsCount > 2) {

        // Thumbnails
        flexsliderThumbs.addClass('is-multiple-items');
        $('.flexslider--thumbs').flexslider({
          animation: 'slide',
          animationSpeed: 200,
          multipleKeyboard: true,
          controlNav: false,
          directionNav: false,
          touch: false,
          itemWidth: 76,
          itemMargin: 3,
          slideshow: false,
          asNavFor: '.flexslider--lrg-img',
          // Do stuff when the slider loads the first slide
          start: function(slider) {
            // Add some ARIA
            slider.container.attr('role', 'listbox');
            slider.slides.attr({'role': 'option', 'aria-selected': 'false'});
            slider.slides.eq(slider.currentSlide).attr('aria-selected', 'true');
          }
        });

        // Large image
        $('.flexslider--lrg-img').flexslider({
          animationSpeed: 50,
          multipleKeyboard: true,
          slideshow: false,
          sync: '.flexslider--thumbs',
          prevText: '<span class="hide-visually">Previous</span>',
          nextText: '<span class="hide-visually">Next</span>',
          // Do stuff when the slider loads the first slide
          start: function(slider) {
            // Add some ARIA
            slider.container.attr('role', 'listbox');
            slider.slides.attr({'role': 'option', 'aria-selected': 'false'});
            slider.slides.eq(slider.currentSlide).attr('aria-selected', 'true');
            slider.controlNav.attr({'href' : '#', 'role' : 'button'});
            slider.directionNav.attr('role', 'button');
            // Inject classes into prev/next buttons
            $('.flexslider--lrg-img').find('.flex-prev').addClass('icon icon--arrow-left-big icon--flush-top'); // prev
            $('.flexslider--lrg-img').find('.flex-next').addClass('icon icon--arrow-right-big icon--flush-top'); // next
          },
          // Do stuff asynchronously with each slider animation
          before: function(slider) {
            $('.flexslider--thumbs').find('.flex-active-slide').attr('aria-selected', 'false');
          },
          // Do stuff after each slider animation completes
          after: function(slider) {
            slider.slides.attr('aria-selected', 'false');
            slider.slides.eq(slider.currentSlide).attr('aria-selected', 'true');
            $('.flexslider--thumbs').find('.flex-active-slide').attr('aria-selected', 'true');
          }
        });

      }

    });
  </script>
<%- end -%>
