<h1>Stores</h1>

<table id="stores">
  <% @stores.each do |store| %>
    <tr>
      <td><%= store.id %></td>
      <td><%= store.name %></td>
      <td><%= store.centre_id %></td>
      <td><%= store.url %></td>
    </tr>
  <% end %>
</table>
