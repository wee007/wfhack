<%= styleguide_intro "Modules", "Modules are the discrete components of the page e.g. carousel, tabs, pagination, etc. This is the meat of the page. Some modules are built on top of Abstractions." do %><% end %>

<%= render 'styleguide/modules/centre_location' %>

<%= render 'styleguide/modules/deal_main' %>

<%= render 'styleguide/modules/drop_down_menu' %>